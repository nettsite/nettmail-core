<?php

namespace Nettsite\NettMail\Core\Contracts;

/**
 * Intentionally empty at this stage. Methods are added incrementally as
 * each domain area (contacts, campaigns, templates, etc.) is implemented,
 * so the contract reflects what the domain actually needs.
 */
interface StorageAdapterContract
{
}
